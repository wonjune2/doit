import 'package:flutter/material.dart';

class Adaptivewidget extends StatelessWidget {
  const Adaptivewidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Flexible(child: Placeholder());
  }
}
